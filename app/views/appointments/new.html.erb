 
<%= form_with model: @appointment do |form| %>
<div class="form-group">
  <%= form.datetime_field(:date_reported, class: "form-control col-sm-3", value: @time_now ) %>
</div>     <%= form.submit %>

<%end %>
